[workspace]
members = [
    "src-tauri",
    "crates/mock-server",
]
resolver = "2"

# Release profile optimizations
[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
strip = true
panic = "abort"
