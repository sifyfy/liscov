// データエクスポート機能のプレースホルダー実装
// Week 17-24で完全実装予定

pub struct DataExporter {
    // 今後実装予定
}

impl DataExporter {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for DataExporter {
    fn default() -> Self {
        Self::new()
    }
}
